import Todo from './Todo.js';

const TodoContainer = () => {
  return (
    <section className="todo-list">
      <ul>
        <Todo />
      </ul>
    </section>
  );
};

export default TodoContainer;
