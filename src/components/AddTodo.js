const AddTodo = () => {
  return (
    <section className="add-todo">
      <input type="text" placeholder="Add a todo..." />
      <button> + </button>
    </section>
  );
};

export default AddTodo;
